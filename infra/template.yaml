AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: BrightEdge Crawler — initial Lambda hello world

Parameters:
  ApiKey:
    Type: String
    NoEcho: true
    Default: ""
    Description: Bearer token required on data endpoints. Leave empty for unauthenticated mode (NOT recommended for public deployments).

Globals:
  Function:
    Timeout: 30
    MemorySize: 1024
    Environment:
      Variables:
        LOG_LEVEL: INFO

Resources:
  ApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: brightedge-crawler-api
      Runtime: python3.12
      Architectures: [arm64]
      CodeUri: ../src/
      Handler: crawler.api.main.handler
      MemorySize: 2048
      Timeout: 28
      Environment:
        Variables:
          PAGES_TABLE: !Ref PagesTable
          JOBS_TABLE: !Ref JobsTable
          RAW_HTML_BUCKET: !Ref RawHtmlBucket
          JOBS_BUCKET: !Ref JobsBucket
          LOG_LEVEL: INFO
          STATIC_QUEUE_URL: !Ref StaticQueue
          HEADLESS_FUNCTION_NAME: !Ref HeadlessWorkerFunction
          API_KEY: !Ref ApiKey
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PagesTable }
        - DynamoDBCrudPolicy: { TableName: !Ref JobsTable }
        - S3CrudPolicy: { BucketName: !Ref RawHtmlBucket }
        - S3CrudPolicy: { BucketName: !Ref JobsBucket }
        - SQSSendMessagePolicy: { QueueName: !GetAtt StaticQueue.QueueName }
        - LambdaInvokePolicy: { FunctionName: !Ref HeadlessWorkerFunction }
      Events:
        ApiRoot:
          Type: HttpApi
          Properties:
            Path: /{proxy+}
            Method: ANY
        ApiBase:
          Type: HttpApi
          Properties:
            Path: /
            Method: ANY

  PagesTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: brightedge-pages
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: url_hash
          AttributeType: S
        - AttributeName: version
          AttributeType: N
        - AttributeName: domain
          AttributeType: S
        - AttributeName: fetched_at
          AttributeType: S
      KeySchema:
        - AttributeName: url_hash
          KeyType: HASH
        - AttributeName: version
          KeyType: RANGE
      GlobalSecondaryIndexes:
        - IndexName: by-domain
          KeySchema:
            - AttributeName: domain
              KeyType: HASH
            - AttributeName: fetched_at
              KeyType: RANGE
          Projection:
            ProjectionType: ALL

  JobsTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: brightedge-jobs
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: job_id
          AttributeType: S
      KeySchema:
        - AttributeName: job_id
          KeyType: HASH

  RawHtmlBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "brightedge-raw-${AWS::AccountId}-${AWS::Region}"
      LifecycleConfiguration:
        Rules:
          - Id: archive-old
            Status: Enabled
            Transitions:
              - StorageClass: STANDARD_IA
                TransitionInDays: 30
              - StorageClass: DEEP_ARCHIVE
                TransitionInDays: 90

  JobsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "brightedge-jobs-${AWS::AccountId}-${AWS::Region}"

  StaticQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: brightedge-static
      VisibilityTimeout: 90
      MessageRetentionPeriod: 345600  # 4 days
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt StaticDLQ.Arn
        maxReceiveCount: 3

  StaticDLQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: brightedge-static-dlq
      MessageRetentionPeriod: 1209600  # 14 days

  StaticWorkerFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: brightedge-crawler-static-worker
      Runtime: python3.12
      Architectures: [arm64]
      CodeUri: ../src/
      Handler: crawler.workers.static_worker.handler
      MemorySize: 1024
      Timeout: 60
      # New AWS accounts have a default account-wide concurrency cap of 10.
      # Cap this consumer at 5 so api + headless still have headroom.
      ReservedConcurrentExecutions: 5
      Environment:
        Variables:
          PAGES_TABLE: !Ref PagesTable
          JOBS_TABLE: !Ref JobsTable
          RAW_HTML_BUCKET: !Ref RawHtmlBucket
          JOBS_BUCKET: !Ref JobsBucket
          CONFIDENCE_THRESHOLD: "0.5"
          HEADLESS_FUNCTION_NAME: !Ref HeadlessWorkerFunction
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PagesTable }
        - DynamoDBCrudPolicy: { TableName: !Ref JobsTable }
        - S3CrudPolicy: { BucketName: !Ref RawHtmlBucket }
        - S3CrudPolicy: { BucketName: !Ref JobsBucket }
        - SQSPollerPolicy: { QueueName: !GetAtt StaticQueue.QueueName }
        - LambdaInvokePolicy: { FunctionName: !Ref HeadlessWorkerFunction }
      Events:
        SqsTrigger:
          Type: SQS
          Properties:
            Queue: !GetAtt StaticQueue.Arn
            BatchSize: 5
            MaximumBatchingWindowInSeconds: 2

  HeadlessWorkerFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: brightedge-crawler-headless-worker
      PackageType: Image
      ImageUri: !Sub "${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/brightedge-headless:latest"
      # sparticuz/chromium only ships x86_64 binaries, so this function
      # overrides the stack's default arm64 architecture.
      Architectures: [x86_64]
      MemorySize: 3008
      Timeout: 60
      Environment:
        Variables:
          PAGES_TABLE: !Ref PagesTable
          RAW_HTML_BUCKET: !Ref RawHtmlBucket
          PLAYWRIGHT_BROWSERS_PATH: /opt/ms-playwright
          HOME: /tmp
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PagesTable }
        - S3CrudPolicy: { BucketName: !Ref RawHtmlBucket }
    Metadata:
      DockerTag: latest
      DockerContext: ../
      Dockerfile: infra/headless.Dockerfile

Outputs:
  ApiUrl:
    Description: HTTP API endpoint
    Value: !Sub "https://${ServerlessHttpApi}.execute-api.${AWS::Region}.amazonaws.com"
